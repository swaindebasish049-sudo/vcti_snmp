package com.networking.ems.snmp.model;

/**
 * The SNMP PROTOCOL version to speak to a device.
 * (Not to be confused with the SNMP4J *library* version in pom.xml.)
 */
public enum SnmpVersion {
    V1,
    V2C,
    V3
}
